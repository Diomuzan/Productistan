<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
            <link rel= "stylesheet" href="{{ asset('/CSS Documents/Style.css') }}">
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
    {% block navbar %}
    <nav class="navbar navbar-expand-lg bg bg-light">
        <div class="container-fluid">
            <span class="navbar-brand h1 heading1 position-relative">Attraction</span>
            <span class="navbar-brand h1 heading2 position-relative">Land</span>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="navItem1 nav-item position-relative">
                        <a class="nav-link position-relative" aria-current="page" href="{{ path('Guest_Home') }}">Home</a>
                    </li>
                    <li class="navItem2 nav-item position-relative">
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <a class="nav-link position-relative" href="{{ path('Logout') }}">Logout</a>
                        {% else %}
                            <a class="nav-link position-relative" href="{{ path('Login') }}">Login</a>
                        {% endif %}
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    {% endblock %}
        {% block body %}{% endblock %}
    </body>
</html>
